{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Papyrus LSP Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "preLaunchTask": "npm: release",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}
