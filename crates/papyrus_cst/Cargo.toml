[package]
name = "papyrus_cst"
version = "0.1.0"
edition.workspace = true

[dependencies]
auto_charset = { path = "../auto_charset" }
bitflags = "2.13.0"
papyrus_token = { path = "../papyrus_token" }

[dev-dependencies]
rayon = "1.12.0"


[features]
# default = ["trace-layout"]
trace-layout = []
