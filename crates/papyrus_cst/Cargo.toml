[package]
name = "papyrus_cst"
version = "0.1.0"
edition.workspace = true

[dependencies]
bitflags = "2.13.0"
papyrus_token = { path = "../papyrus_token" }

[features]
# default = ["trace-layout"]
trace-layout = []
