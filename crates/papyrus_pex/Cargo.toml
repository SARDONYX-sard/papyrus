[package]
name = "papyrus_pex"
description = "Convert struct to .pex"
version = "0.1.0"
edition.workspace = true

[dependencies]
bitflags = "2.13.0"

[features]
# default = ["trace-layout"]
trace-layout = []
